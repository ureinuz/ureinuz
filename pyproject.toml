[project]
name = "ureinuz"
version = "0.0.1"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "jax>=0.10.2",
]

[dependency-groups]
dev = [
    "equinox>=0.13.8",
    "flax>=0.12.7",
    "optax>=0.2.8",
]
